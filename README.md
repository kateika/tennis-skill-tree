# Tennis skill tree builder

### Demo

https://github.com/user-attachments/assets/4a253f48-caac-44cc-9865-be349a4120b4

### Instalation steps

1. Clone this repository to destination folder.

`git clone https://github.com/kateika/tennis-skill-tree.git`

2. Install dependencies.

`npm install`

3. Build and Run local environment.

`npm run build && npm run start`

4. The app is going to be available at "http://localhost:3333"

### List of completed bonuses
1. Added prevention of circular dependencies

### Disclosure
1. Repo setup was done based on https://github.com/rkoziol/react-typescript-jest-boilerplate.git
2. GitHub Copilot was used for some parts of the code/autocompletion

### Available scripts

`npm start` - run development server with hot reload mode,

`npm build` - build distributon package,

`npm run lint` - run lints,

`npm run lint:code` - run lint for typescript / javascript files,

`npm run lint:code:fix` - run lint for typescript / javascript files with auto fix option,

`npm run lint:style` - run lint for style files,

`npm run lint:style:fix` - run lint for style files with auto fix option,

`npm run test:jest` - run Jest tests,

`npm run test:jest:watch` - run Jest tests with watch mode,

`npm run test:jest:coverage` - run Jest tests and generate coverage report.

### In case the app doesn't run, these were my library/tools versions:
Node version: 20.14.0
NPM version: 10.7.0
